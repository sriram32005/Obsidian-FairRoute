---
title: LLM Query Scheduling with Prefix Reuse and Latency Constraints
authors:
  - Gregory Dexter
year: "2025"
venue: 39th Conference on Neural Information Processing Systems (NeurIPS 2025
paper_type: research
status: unread
rating:
url: ""
pdf: "[[2025 - kLPM.pdf]]"
tags:
---


# 2025 - kLPM

> [!abstract] One-Line Summary  
> Write the entire paper's contribution in **1–2 sentences**.

---

## 1. Paper Metadata

| Field                   | Notes                                                                   |
| ----------------------- | ----------------------------------------------------------------------- |
| **Authors**             |                                                                         |
| **Year**                | 2025                                                                    |
| **Venue**               | 39th Conference on Neural Information Processing Systems (NeurIPS 2025) |
| **Paper Type**          | Theoratical Algorithm which combines FCFS and LPM                       |
| **Code Available?**     | Yes / No                                                                |
| **Artifact Available?** | Yes / No                                                                |
| **Related System**      | vLLM / Sarathi / Custom / Other                                         |

---

# 2. Problem

## Problem Statement

**What precise problem does this paper solve?**

In autoregressive LLMs, prompts with shared prefixes can leverage the intermediate representations (i.e. key and value tensors, often stored in the key-value (KV) cache) of the common prefix, reducing both memory usage and computational overhead during inference.

RadixAttention, which stores the KV cache of processed queries and automatically reuses shared prefixes by maintaining a radix tree Zheng et al. (2024). This tree tracks cached values and matches them with new incoming prompts, enabling efficient reuse without requiring manual intervention. 

Automatic prefix reuse enabled by RadixAttention can substantially improve goodput in many realistic settings, particularly when there is large overlap among query prompts. However, under a stream of queries, the performance of RadixAttention becomes dependent on the order in which queries are processed (i.e., the “schedule”), and its implications for latency remain poorly understood.

The main scheduling algorithms considered in prior work are 
	1. First Come First Serve (FCFS) :  which processes queries in arrival order
	2. Longest Prefix Match (LPM) : which greedily maximizes KV cache reuse at each scheduling step
In offline setting ( where all the queries are known in advance), LPM indeed maximizes cache reuse. 
The online setting, where queries arrive over time under tight time-to-first-token constraints, was not characterized.

## Why Does This Problem Matter?

we provide a theoretical exploration of RadixAttention scheduling in the online regime, focusing on non-preemptive scheduling.

We design a mechanism that exploits the benefits of LPM but mitigates its performance risks in high-load scenarios, resulting in superior performance for long-prompt, short-output (i.e., prefill-dominant) queries where Radix- Attention delivers significant efficiency gains.

---

# Algorithm

![[Pasted image 20260902193650.png]]

The intuition behind Algorithm 1 is that it first performs k greedy prefix-match steps in the spirit of LPM to maximize prefix reuse. After these k steps, it processes the oldest query in the queue, mirroring FCFS. This strategy circumvents the LPM failure case, where a query could be unprocessed if its prompt never have sufficiently high prefix overlap. At the same time, it retains the significant prefix-reuse advantage that LPM provides
 
---

# Experiment Setup

- Model:  Llama-3.1-8B-Instruct model
- GPU Cluster: 8 x A100 GPUs
- Benchmakr: SGLangs Serving benchmark
- Dataset: 4 Prompts with shared user history from the 8k context length prompts for 2100 prompts in total

---

# Key Results & Observations

Metric: p99 TTFT

![[Pasted image 20260902194207.png]]


Observations:
	1. k-LPM consistently outperforms both FCFS and LPM. Over a wide range of request rates considered, k-LPM achieves a lower TTFT. This underscores its robust advantage, especially under moderate to heavy loads. Additionally, we note that large values of k (e.g., k = 1000) behave like k = ∞
	2. FCFS shows better performance at low request rates, while LPM is advantageous at higher rates.
	3. k-LPM performance remains robust under realistic models and prompt data, even when theoretical assumptions are relaxed.


---

